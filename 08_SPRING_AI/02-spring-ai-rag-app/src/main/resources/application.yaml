spring:
  application:
    name: 02-spring-ai-rag-app
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o
          max-tokens: 1000
    vectorstore:
      pinecone:
        api-key: ${PINECONE_API_KEY}
        index-name: ashokitaidemo
